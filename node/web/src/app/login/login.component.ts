import { Component } from '@angular/core'

@Component({
    selector: 'app-logon',
    templateUrl: './login.component.html',
    styleUrls: ['./login.component.css']
})

export class Login {
    handleClick(value) {
    }
}